import React, { Component } from 'react';

class RemoveRowButton extends Component {

    // Show this component on the DOM
    render() {
        return (
            <div>
                <p>[ RemoveRowButton ]</p>
            </div>
        );
    }
}

export default RemoveRowButton;