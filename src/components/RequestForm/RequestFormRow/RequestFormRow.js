import React, { Component } from 'react';

class RequestFromRow extends Component {

    // Show this component on the DOM
    render() {
        return (
            <div>
                <p>[ RequestFromRow ]</p>
            </div>
        );
    }
}

export default RequestFromRow;